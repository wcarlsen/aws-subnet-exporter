apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aws-subnet-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "aws-subnet-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aws-subnet-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
